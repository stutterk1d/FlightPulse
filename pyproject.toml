[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --tb=short"

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [".venv", "logs", "data", "reports", "mlruns"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
